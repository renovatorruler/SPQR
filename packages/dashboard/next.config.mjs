/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // ReScript compiles .res → .res.mjs in src/
  // Next.js App Router lives in src/app/
}

export default nextConfig
